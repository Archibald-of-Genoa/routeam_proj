import Card from "../Card/Card";
import { CardsContainer, HeroContainer } from "./Hero.styled";

const Hero = () => {
  return (
    <HeroContainer>
      <CardsContainer>

      </CardsContainer>
    </HeroContainer>
  );
};

export default Hero;
